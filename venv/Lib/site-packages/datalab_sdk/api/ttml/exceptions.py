from datalab_sdk.api.exceptions import InputNotValid, ResourceNotFound

class TtmlNotFound(ResourceNotFound):
    """Raised when the requested info not found in Ttml"""
    def __init__(self, correlation_id: str = None):
        super().__init__(f"The requested info for ttml not found", correlation_id)