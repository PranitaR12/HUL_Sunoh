from marshmallow_dataclass import dataclass
from typing import List
from dataclasses import field
from datalab_sdk.api.tool_inventory.types.shared.enums import ToolType
from datalab_sdk.api.tool_inventory.types.responses.tool_version import ToolVersion

@dataclass
class ToolInfo:
    id: str
    tool_type: ToolType = field(metadata=dict(by_value=True))
    name: str
    description: str
    owner: str
    group: str
    versions: List[ToolVersion]
    